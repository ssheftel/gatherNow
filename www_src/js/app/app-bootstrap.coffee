###
  Manuel Application Bootstrapping Code - Inserted just before end of body tag
###

# ------------------------------Main Code--------------------------------------

appName = 'gatherNow'
eventName = 'deviceready'

# bootstrapApp = ->
#   angular.bootstrap(document.body, [appName])
#   return

# do ->
#   console.log('bootstraping') if console?.log?
#   document.addEventListener(eventName, bootstrapApp, false)
#   return
